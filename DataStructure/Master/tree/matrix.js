// https://blog.csdn.net/daaikuaichuan/article/details/80586408
// https://blog.csdn.net/zjw_python/article/details/85223145
var networkDelayTime = function (times) {
    
};
const MARK = 'x';
const traverseMatrix = (N, items) => {
    const length = N;
    let graps = new Array(length);
    for (let i = 0 ; i < length ; i++) {
        const rows = new Array(length);
        for (let j = 0; k < length; j++) {
            rows[j] = MARK;
        }
        graps[i] = rows;
    }
    for (let i = 0 ; i < items.length ; i++){

    }
    return graps;
}

function sum(str1, str2) {
     
}